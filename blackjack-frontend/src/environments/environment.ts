/**
 * Build de producción: si el frontend y el API no comparten el mismo dominio,
 * pon aquí la URL base del backend (sin barra final), p. ej. https://api.midominio.com
 */
export const environment = {
  production: true,
  apiOrigin: 'http://localhost:8080',
};
